import NetworkExtension;

// Fill this in for iOS support

class PacketTunnelProvider : NEPacketTunnelProvider { 
    override func startTunnelWithOptions(options: [String : NSObject]?, completionHandler: (NSError?) -> Void) {
        
    }
}

